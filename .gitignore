.claude/
.DS_Store
node_modules/
.cache/

# secrets — never commit
.env
.env.*
!.env.example

# classifier model + embedding cache (rebuilt from posts.json + rejected.json)
classifier/model.npz
classifier/embeddings_cache.npz
classifier/__pycache__/

# video renders + ephemeral composition state (all regenerated on demand)
presentations/renders/
presentations/audio/
presentations/assets/
presentations/transcript.json
presentations/index.html
